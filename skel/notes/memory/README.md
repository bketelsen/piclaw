# Notes-memory compatibility directory

This directory is kept for note-oriented compatibility workflows.

Primary durable agent memory now lives under `~/.piclaw/cog/memory/`, not `notes/memory/`.

Use `notes/daily/` for human-readable day logs. Only keep compatibility artifacts or sparse note-derived outputs here when an older workflow still expects them.
